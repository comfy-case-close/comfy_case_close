package com.fnbx.integration.enums;

/**
 * Matches PostgreSQL enum {@code shared.pos_vendor}.
 * Adding a vendor means one constant here plus one PosAdapter class.
 */
public enum PosVendor { IPOS, KIOTVIET, SAPO, HARAVAN, MANUAL }
