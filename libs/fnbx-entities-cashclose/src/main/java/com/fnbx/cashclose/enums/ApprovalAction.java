package com.fnbx.cashclose.enums;

/** Matches PostgreSQL enum {@code shared.approval_action}. Action on a whole close. */
public enum ApprovalAction { SUBMIT, APPROVE, REJECT, REQUEST_CHANGES, VOID }
